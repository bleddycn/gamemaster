# Azure SQL connection string (sqlserver provider)
# Replace placeholders and copy to .env (DO NOT COMMIT real secrets)
DATABASE_URL="sqlserver://<USER>:<PASSWORD>@<SERVER>.database.windows.net:1433;database=<DB_NAME>;encrypt=true;trustServerCertificate=false"
SHADOW_DATABASE_URL="sqlserver://<USER>:<PASSWORD>@<SERVER>.database.windows.net:1433;database=<DB_NAME_SHADOW>;encrypt=true;trustServerCertificate=false"

# Fastify
PORT=4000

# JWT Secret - use a strong, random string in production
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"